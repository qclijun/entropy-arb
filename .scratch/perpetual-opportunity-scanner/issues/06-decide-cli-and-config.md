# Decide scanner CLI and configuration contract

Type: grilling
Status: open
Blocked by:

## Question

What exact `tools/scan.py` arguments, `scanner:` configuration schema, defaults, override precedence, validation errors, and startup/exit behavior make the agreed scanner behavior explicit without changing the existing trading CLI?
